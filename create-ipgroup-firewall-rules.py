
# get session host user assignment with VM private IP from resource graph

# for each record in RGQuery, create IP Group if not exist, IP Group name will be username without "@...com"

# create IP group for each on-premise subnets

# with a pre-formatted user <-> onprem-subnet mapping list, read the list and create network rule accordingly 